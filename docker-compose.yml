services:
  ebdm:
    build: .
    volumes:
      - .:/app
    environment:
      - OMP_NUM_THREADS=8
      - MKL_NUM_THREADS=8
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
